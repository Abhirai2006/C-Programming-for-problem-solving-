#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "OpenAI GPT";
    int len = strlen(str);
    printf("String: %s\n", str);
    printf("Length: %d\n", len);
    return 0;
}
