#include <stdio.h>
#include <stdbool.h>

int main() {
    /*signed int a = -2;
    unsigned int b = -2;

    printf("Signed int a: %d\n", a);
    printf("Unsigned int b: %u\n", b); // Large positive number due to underflow

    printf("Size of unsigned short: %zu bytes\n", sizeof(unsigned short));

    return 0; */

    // Binary representation 2 is 10

    /*int num = 256;
    printf("Binary representation of %d (32-bits):\n", num);

    for (int i = 31; i >= 0; i--) {
        printf("%d", (num >> i) & 1); // Extract and print each bit
    }

    printf("\n");
    return 0;*/
    float div;
    div = ((float)9/2);// type conversion
    printf("%.2f",div);

}
