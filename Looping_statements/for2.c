#include <stdio.h>
void main() {
    // Print numbers from 1 to 5 using a for loop without braces
    for (int i = 1; i <= 5; i++)
        printf("%d\n", i); // Single statement in loop
        printf("ABC"); // only one line is treated
}
